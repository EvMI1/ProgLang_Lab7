# Фадеев Михаил БАС-1-24 Лабораторная №7

# Задание 1

## Задача 7

### Текст задачи

Подсчитать количество вхождений максимального элемента в файл.

### Алгоритм решения

Реализовано в классе `Task1_5`:

- метод `FillFile(string fileName, int count)` — заполняет текстовый файл случайными целыми числами в диапазоне [-100, 100], по одному в строке;
- приватный метод `FindMaxElement(string fileName)` — находит максимальный элемент за один проход по файлу;
- публичный метод `CountMax(string fileName)` — находит максимум, затем подсчитывает количество его вхождений (второй проход).

Для работы с файлом используются `StreamReader` и `StreamWriter`. Потоки корректно закрываются через `using`.
![]()
### Тестирование

![](https://github.com/EvMI1/ProgLang_Lab7/blob/main/task/screenshots/1.png)


# Задание 2

## Задача 7

### Текст задачи

Вычислить количество чётных элементов.

### Алгоритм решения

Реализовано в классе `Task1_5`:

- метод `FillFile2(string fileName, int rowCount, int colCount)` — заполняет файл случайными числами [-100, 100], по `colCount` чисел в строке, разделённых пробелом;
- метод `CountEvenElements(string fileName)` — читает файл построчно, разбивает каждую строку через `Split(' ')` и считает элементы, удовлетворяющие условию `element % 2 == 0`.

### Тестирование

![](https://github.com/EvMI1/ProgLang_Lab7/blob/main/task/screenshots/2.png)

# Задание 3

## Задача 7

### Текст задачи

Переписать в другой файл строки, содержащие заданную комбинацию символов. Например,
строка «Сегодня старшеклассники выполняли ЕГЭ по информатике и ИКТ» содержит
комбинацию «форма».

### Алгоритм решения

Реализовано в классе `Task1_5`:

- метод `CopyLinesWithSubstring(string sourceFileName, string destFileName, string substring)` — читает исходный файл через `StreamReader`, проверяет каждую строку методом `Contains(substring)` и записывает подходящие строки в файл результата через `StreamWriter`.

### Тестирование

![](https://github.com/EvMI1/ProgLang_Lab7/blob/main/task/screenshots/31.png)
![](https://github.com/EvMI1/ProgLang_Lab7/blob/main/task/screenshots/32.png)
![](https://github.com/EvMI1/ProgLang_Lab7/blob/main/task/screenshots/33.png)

# Задание 4

## Задача 7

### Текст задачи

Найти разность максимального и минимального элементов заданного файла.

### Алгоритм решения

Реализовано в классе `Task1_5`:

- метод `FillBinaryFile(string fileName, int count)` — заполняет бинарный файл через `BinaryWriter`;
- приватные методы `FindMaxElementBinary` и `FindMinElement` — читают файл через `BinaryReader` и находят максимум/минимум;
- метод `GetMaxMinDifference(string fileName)` — возвращает разность `max - min`.

Используются `FileStream` + `BinaryReader`/`BinaryWriter`.

### Тестирование

![](https://github.com/EvMI1/ProgLang_Lab7/blob/main/task/screenshots/41.png)
![](https://github.com/EvMI1/ProgLang_Lab7/blob/main/task/screenshots/42.png)

# Задание 5

## Задача 7

### Текст задачи

Файл содержит сведения об игрушках: название игрушки, ее стоимость в рублях и возрастные
границы (например, игрушка может предназначаться для детей от двух до пяти лет). Вывести
названия наиболее дорогих игрушек (цена которых отличается от цены самой дорогой игрушки
не более, чем на k руб.).

### Алгоритм решения

Объявлена структура `Toy` с полями `Name`, `Price`, `AgeFrom`, `AgeTo`.

Реализовано в классе `Task1_5`:

- метод `FillToysFile(string fileName)` — генерирует 10 случайных игрушек и сериализует список в XML через `XmlSerializer`;
- метод `ReadToysFile(string fileName)` — десериализует список из XML;
- метод `GetMostExpensiveToys(string fileName, int k)` — находит максимальную цену и возвращает названия игрушек, удовлетворяющих условию `maxPrice - Price <= k`.

### Тестирование

![](https://github.com/EvMI1/ProgLang_Lab7/blob/main/task/screenshots/5.png)

# Задание 6

## Задача 7

### Текст задачи

Составить программу, которая переносит в конец непустого списка L его первый элемент.

### Алгоритм решения

Реализовано в классе `Task6_10`:

- метод `MoveFirstToEnd(List<int> list)` — сохраняет первый элемент, удаляет его через `RemoveAt(0)` и добавляет в конец списка через `Add()`.

Проверяется случай пустого списка.

### Тестирование

![](https://github.com/EvMI1/ProgLang_Lab7/blob/main/task/screenshots/6.png)

# Задание 7

## Задача 7

### Текст задачи

Из списка L, содержащего не менее двух элементов, удалить все элементы, у которых одинаковые
«соседи» (первый и последний элементы считать соседями).

### Алгоритм решения

Реализовано в классе `Task6_10`:

- метод `RemoveEqualNeighbours(LinkedList<int> list)` — проходит по списку с помощью `LinkedListNode`, проверяет условие равенства предыдущего и следующего элемента и удаляет текущий узел при необходимости.

Удаление происходит безопасно (без нарушения итерации). Проверяется случай, когда элементов меньше 2.

### Тестирование

![](https://github.com/EvMI1/ProgLang_Lab7/blob/main/task/screenshots/71.png)
![](https://github.com/EvMI1/ProgLang_Lab7/blob/main/task/screenshots/72.png)
![](https://github.com/EvMI1/ProgLang_Lab7/blob/main/task/screenshots/73.png)

# Задание 8

## Задача 7

### Текст задачи

Есть перечень мебельных фабрик, продукция которых представлена в мебельном магазине.
Известно, мебель каких фабрик приобреталась n покупателями. Определить для каждой фабрики,
мебель каких из них приобреталась всеми покупателями, каких — некоторыми из покупателей, и
каких — никем из покупателей.

### Алгоритм решения

Реализовано в классе `Task6_10`:

- метод `AnalyzeFurnitureShops(List<HashSet<string>> purchases, HashSet<string> allFactories)` — использует операции над множествами (`IntersectWith`, `UnionWith`, `ExceptWith`);
- вспомогательный метод `PrintSet(HashSet<string> set)` — красиво выводит содержимое множества или сообщение «(пусто)».

`HashSet` обеспечивает эффективную работу с уникальными элементами и быстрые операции пересечения/объединения/разности.

### Тестирование

![](https://github.com/EvMI1/ProgLang_Lab7/blob/main/task/screenshots/8.png)

# Задание 9

## Задача 7

### Текст задачи

Файл содержит текст на русском языке. Напечатать в алфавитном порядке все глухие согласные
буквы, которые входят в каждое нечетное слово и не входят хотя бы одно четное слово.

### Алгоритм решения

Реализовано в классе `Task6_10`:

- множество глухих согласных: `{п, ф, к, т, ш, с, х, ч, щ, ц}` (в нижнем регистре);
- текст читается целиком, разбивается на слова с помощью `Split` (учитываются пробелы, знаки препинания);
- для нечётных слов вычисляется пересечение глухих согласных (буквы, входящие во все нечётные слова);
- для чётных слов собираются глухие согласные, которые встречаются хотя бы в одном;
- результат — разность этих двух множеств, отсортированный по алфавиту.

### Тестирование

![](https://github.com/EvMI1/ProgLang_Lab7/blob/main/task/screenshots/91.png)
![](https://github.com/EvMI1/ProgLang_Lab7/blob/main/task/screenshots/92.png)

# Задание 10

## Задача 7

### Текст задачи

В молочных магазинах города Х продается сметана с жирностью 15, 20 и 25 процентов. В городе
X был проведен мониторинг цен на сметану. Напишите эффективную по времени работы и по
используемой памяти программу, которая будет определять для каждого вида сметаны, сколько
магазинов продают ее дешевле всего. На вход программе сначала подается число магазинов N. В
каждой из следующих N строк находится информация в следующем формате:
<Фирма><Улица><Жирность><Цена>
где <Фирма> – строка, состоящая не более, чем из 20 символов без пробелов, <Улица> – строка,
состоящая не более, чем из 20 символов без пробелов, <Жирность> – одно из чисел – 15, 20 или
25, <Цена> – целое число в диапазоне от 2000 до 5000, обозначающее стоимость одного литра
сметаны в копейках. <Фирма> и <Улица>,<Улица> и <Жирность>, а также <Жирность> и <Цена>
разделены ровно одним пробелом. Пример входной строки:
Перекресток Короленко 25 3200
Программа должна выводить через пробел 3 числа – количество магазинов, продающих дешевле
всего сметану с жирностью 15, 20 и 25 процентов. Если какой-то вид сметаны нигде не
продавался, то следует вывести 0.
Пример выходных данных:
12 10 0

### Алгоритм решения

Реализовано в классе `Task6_10`:

- метод `AnalyzeSourCreamPrices(string fileName)` — читает файл построчно, парсит жирность и цену;
- для каждой жирности (15, 20, 25) отслеживается минимальная цена и количество товаров с этой ценой;
- в конце выводятся три числа через пробел.

Формат строки в файле предполагается: `Магазин Производитель Жирность Цена`.

### Тестирование

![](https://github.com/EvMI1/ProgLang_Lab7/blob/main/task/screenshots/10.png)

